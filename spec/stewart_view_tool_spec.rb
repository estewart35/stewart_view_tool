require "spec_helper"

describe StewartViewTool do
  it "has a version number" do
    expect(StewartViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
